This may be all covered here: https://webpack.academy/p/the-core-concepts 

Webpack is a module (AMD, CommonJS) and asset (CSS, fonts, images) bundler, creating browser-understandable code (replaces `require()` statements).

It can also split your bundles into multiple files, and remove any dead code.
